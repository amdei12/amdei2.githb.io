
// pointsArr=[{id:1,point:[10,8,6,2,2]},{id:2,point:[0,4,2,0,0]}];
// function Stotal(ss){

const { Z_HUFFMAN_ONLY } = require("zlib");

    

// }

// function gettotal(pointsArr){
// for(const key of pointsArr){
//     if((pointsArr[key]==Array)){
//         function Stotal(pointsArr[key])
//     }
      
//     }
    
// }
// }
function sumn(n){
    sum=0;
     
      for(let i=0;i<n;i++){
    let x=prompt("enter the number");
      sum=sum+parseInt(x);
    }
    return sum;
    }
    alert(sumn(5));
    
    function summb(){
        let arr=[];
        let i=0;
        while(true){
            let valu=prompt("enter value");
            valu=parseInt(valu);
            if(typeof(valu)!="number"){
             break;
            }else{
                arr[i]=y;
                i++;
            }
        }
        return arr;
    } 
    function abc(){
        let i=0;
        let sum=0;
        while(true){
            ++i;
            sum+=i;
            
        }
        return sum;
    }
    function  getnumber(n){
        let arr=[];
        for(let i=0;i<n;i++){
            let y=0;
              
              while(true){
            let x=Math.random()*100;
            if(x>y){
              y=x;
              arr[i]=y;
              break;
               }
        }
    }